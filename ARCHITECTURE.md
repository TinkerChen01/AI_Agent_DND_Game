# 兰斯的冒险 — 架构说明

> 本文档记录项目的文件体系与架构演变，供开发者和玩家理解游戏运行机制。
> 最后更新：2026-05-18

---

## 一、项目概述

基于 **D&D 5e** 的 AI Agent 单人跑团游戏，使用 **L1-L7 文件框架** 管理游戏状态，由 **Hermes Agent** 驱动。

- 玩家角色：**兰斯**，人类野蛮人（狂战士）Lv.4
- 队友 NPC（AI 扮演）：**卡芙卡**，半精灵邪术士（咒剑士）Lv.4
- 当前模组：**丝线回廊**（短团，单章）

---

## 二、架构演变

### 旧架构（Claude Code 时代）

```
CLAUDE.md          ← AI 行为规则（项目级指令，自动加载）
L0_DM手册.md       ← DM 行为总纲（完整参考）
.claude/skills/    ← 6 个 Claude Code Skill（存档/战斗/查询/检查点/模组扩展/节点）
L1 ~ L7            ← 数据文件
```

**运行方式**：Claude Code 打开项目时自动读取 CLAUDE.md，获得行为规则和文件引用链。游戏过程中通过 `/存档`、`/战斗开始` 等 slash command 调用 `.claude/skills/` 中的流程。

**问题**：该体系依赖 Claude Code 的 CLAUDE.md 自动加载机制。切换至 Hermes Agent 后，CLAUDE.md 不再被自动识别，行为规则无法注入。

### 新架构（Hermes Agent 时代）

```
~/.hermes/skills/
├── dnd-dm              ← 主 Skill：行为总纲（合并 CLAUDE.md + L0 全部规则）
├── dnd-存档             ← 子 Skill：完整存档流程
├── dnd-战斗开始          ← 子 Skill：战斗初始化
├── dnd-查询             ← 子 Skill：跨文件情报检索
├── dnd-检查点           ← 子 Skill：轻量状态同步
├── dnd-模组扩展          ← 子 Skill：模组动态扩展
└── dnd-节点             ← 子 Skill：追加 L6 节点

项目目录/
├── L1_世界设定.md ~ L7_城市休整与随机遭遇.md  ← 数据层
└── 备份文件夹（详见 §六）
```

**运行方式**：用户在 Hermes Agent 中输入 `/skill dnd-dm` 加载主 Skill，行为规则注入 system prompt，全程有效。L1-L7 为纯数据文件，按需读取。

### 关键变化

| 项目 | 旧 | 新 | 原因 |
|------|----|----|------|
| 行为规则载体 | CLAUDE.md（项目文件） | `dnd-dm` Skill（Hermes skill） | Hermes 不识别 CLAUDE.md |
| 操作技能 | `.claude/skills/`（Claude 格式） | `dnd-*` 系列（Hermes 格式） | 技能格式不兼容 |
| L0 内容 | 独立文件 | 合并入 `dnd-dm` Skill | 减少文件依赖，注入优先级更高 |
| 规则优先级 | 对话上下文（可被压缩） | System Prompt（不可压缩） | Skill 注入优先级更高 |
| 项目文件引用 | `详见 L0「3.4」` | `详见 dnd-dm Skill「5.4」` | 跟随架构变化 |

---

## 三、游戏运行机制

### 3.1 启动流程

```
用户输入 "/skill dnd-dm"
  │
  ▼
dnd-dm Skill 注入 Hermes system prompt
  │
  ▼
AI 读取 L6 → 叙事摘要、前情提要、存档快照
  │
  ▼
AI 读取 L1 → L2 → L3 → L4 → L5（按需读取 L7）
  │
  ▼
合成战役梗概 → 从存档快照直接接续叙事
```

### 3.2 游戏模式

| 开场方式 | 模式 | 行为 |
|---------|------|------|
| "游戏继续"、角色行动/对话 | 游戏模式 | AI 担任 DM，按 dnd-dm 规则主持 |
| "游戏暂停"、讨论架构/规则 | 助手模式 | 普通助手状态 |
| "存档"、"记一下"、"保存" | 存档指令 | 执行完整存档流程 |

### 3.3 Skill 调用链

```
游戏过程中玩家触发事件
  │
  ▼
AI 执行 dnd-dm 中的强制检查点自检
  │
  ├─ 命中战斗触发 → 调用 dnd-战斗开始 流程
  ├─ 命中存档触发 → 调用 dnd-存档 流程
  ├─ 命中节点触发 → 调用 dnd-节点 流程
  ├─ 命中状态变更 → 调用 dnd-检查点 流程
  └─ 玩家主动查询 → 调用 dnd-查询 流程
```

所有 Skill 已在 dnd-dm 主 Skill 中完整定义其流程，AI 可直接执行。子 Skill 提供聚焦的操作指引。

---

## 四、文件体系（L1-L7）

| 文件 | 用途 | 更新频率 |
|------|------|---------|
| [L1_世界设定.md](L1_世界设定.md) | Forgotten Realms 世界观（静态） | 极少 |
| [L2_模组框架.md](L2_模组框架.md) | 当前模组结构/NPC/战斗设计 | 跨模组 |
| [L3_人物卡.md](L3_人物卡.md) | 角色身份锚定 | 质变才更新 |
| [L4_角色状态.md](L4_角色状态.md) | HP/资源/装备/关系 | 战斗结束/休整/获得物品 |
| [L4b_战斗日志.md](L4b_战斗日志.md) | 战斗中临时文件 | 战斗期间 |
| [L5_世界状态.md](L5_世界状态.md) | 场景/时间/flag/势力 | 场景切换/flag变化 |
| [L6_冒险笔记.md](L6_冒险笔记.md) | 叙事记忆/节点/前情提要 | 关键事件/存档/会话结束 |
| [L7_城市休整与随机遭遇.md](L7_城市休整与随机遭遇.md) | 城市设施/休整规则/随机遭遇表 | 玩家选择城市休整时 |

> 所有数据文件的写入触发规则和格式均定义在 `dnd-dm` Skill 中，不依赖 L0_DM手册.md。

---

## 五、Skill 体系

### 主 Skill

| Skill | 位置 | 功能 |
|-------|------|------|
| **dnd-dm** | `~/.hermes/skills/dnd-dm/` | D&D 5e AI 跑团 DM 规则总纲。包含模式切换、会话启动、叙事规则、数据同步协议、战斗流程、关系判定、回滚协议、行为红线。 |

### 子 Skill

| Skill | 功能 | 触发时机 |
|-------|------|---------|
| dnd-存档 | 完整存档流程（同步 L4/L5/L6 + 生成前情提要） | 玩家说"存档"/会话结束 |
| dnd-战斗开始 | 战斗初始化（读状态、投先攻、写 L4b） | 战斗即将开始 |
| dnd-查询 | 跨文件关键词检索 | 玩家查 NPC/地点/物品 |
| dnd-检查点 | 轻量级状态同步（不生成前情提要） | 探索/社交后手动触发 |
| dnd-模组扩展 | 在 L2 空缺处动态扩展场景/支线 | 玩家想去未设计区域 |
| dnd-节点 | 按 L6 格式追加冒险笔记节点 | 命中写入条件需记录 |

---

## 六、项目目录说明

### 活跃文件

仅 L1-L7 和素材库文件在根目录。AI 按需读取这些文件。

### 备份文件夹（Git 保留，AI 无需读取）

这些文件夹保留在项目中以便 Git 存档，但 AI 在游戏过程中不会读取它们：

| 文件夹 | 内容 |
|--------|------|
| `技能文件备份（AI不需要读此文件）` | 7 个 Hermes Skill 的副本（`.claude/skills/` 转换而来） |
| `旧版规则文件备份（AI不需要读此文件）` | CLAUDE.md、L0_DM手册.md、原始 `.claude/skills/` |
| `旧模组（AI不需要读此文件）` | 已完成模组「磐石堡残响」的旧版框架文件 |
| `重要NPC备份（AI不需要读此文件）` | 重要 NPC 档案的旧版副本 |
| `冒险故事书（AI不需要读此文件）` | 叙事性记录（非结构化数据） |
| `功能性扩展说明书（AI不需要读此文件）` | 扩展功能说明文档 |

> 要从备份恢复 Skill：将 `技能文件备份` 中的文件夹复制到 `~/.hermes/skills/` 即可。

---

## 七、快速上手

### 首次运行

```bash
# 1. 将备份的 Skill 复制到 Hermes
cp -r 技能文件备份/* ~/.hermes/skills/

# 2. 启动 Hermes
hermes

# 3. 加载 DM 规则
/skill dnd-dm

# 4. 开始游戏
游戏继续
```

### 日常运行

```bash
hermes --continue   # 继续上次会话
# 然后
/skill dnd-dm       # 加载 DM 规则
游戏继续            # 开始
```

> `dnd-dm` 加载后即注入 system prompt，全程有效无需重复加载。新会话需重新 `/skill dnd-dm`。

---

## 八、变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-05-18 | 2.0 | 架构从 Claude Code 迁移至 Hermes Agent。CLAUDE.md + L0 合并为 `dnd-dm` Skill，`.claude/skills/` 转换为 6 个 Hermes 子 Skill。L1-L7 中的 L0 引用全部更新为 `dnd-dm` 引用。旧文件统一归档至备份文件夹。 |
