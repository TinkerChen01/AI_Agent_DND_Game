# L6 — 冒险笔记

> 记忆系统：
> 1. **过往冒险摘要** — 已完成模组的一句话概括，跨模组保留。AI 启动时扫一眼即知角色经历。
> 2. **前情提要** — 包含”叙事摘要”（游戏内视角快速定位）、”前情提要”（含角色互动纹理的完整回顾）和”存档快照”（存档时游戏的实际场景、对话、正在做的行动），用于恢复”上次的感觉”
> 3. **节点记录** — 结构化事件日志，用于精确查询”上次发生了什么”
>
> **节点由 AI 按 L0 同步协议自动追加。前情提要在玩家下达”存档”指令或会话结束时生成/更新。**
>
> **生成规则**：每次生成前情提要前，AI 必须读取最近 5-8 个 L6 节点，确保所有 `key_events`、`unlock_flags` 和 `dm_notes` 都在摘要中得到体现。

---

## 过往冒险摘要

> 已完成模组的一句话归档。只增不减，跨模组保留。AI 启动时读取此表快速定位角色经历。

| 模组 | 一句话概括 |
|------|-----------|
| 磐石堡残响 | 兰斯和卡芙卡从占据废弃矮人要塞的前骑士奥德里克手中救出了无冬城贵族小姐伊芙琳，途中与副官格鲁什”铁颚”交战并将其斩杀 |

---

## 战役信息

| 项目 | 内容 |
|------|------|
| 战役名称 | 待定 |
| 起始日期 | — |
| 起始地点 | — |
| 玩家角色 | 兰斯（人类野蛮人 Lv.4）/ 卡芙卡（半精灵邪术士 Lv.4） |

---

## 会话索引

> AI 在生成新节点前，先读取本表确认当前会话序号。

| 会话 | 现实日期 | 游戏内日期 | 节点范围 |
|------|---------|-----------|---------|
| — | — | — | — |

---

## 节点 ID 生成规范

### 格式
`S{Session}N{Seq}`

| 字段 | 含义 | 如何确定 |
|------|------|---------|
| Session | 真实世界跑团会话序号 | 读取本文件最后一个节点的 Session；新会话则 +1 |
| Seq | 本场会话内的节点序号 | 同一会话则 +1，新会话重置为 1 |

### 连续性保障
1. 每次生成节点前，查看 L6 最后一个节点的 `node_id`
2. 如果当前是同一会话（未关闭聊天/未隔日），Seq 递增
3. 如果是新会话（玩家说"今天继续"或隔日启动），Session 递增，Seq 重置为 1
4. 生成后，更新上方「会话索引」表

### 拆分 vs 合并规则
- **默认：一触发一节点。** 每次命中 L6 写入条件，新建独立 node。
- **例外可合并**：若该事件与前一个节点满足以下全部条件，可追加到前一个节点的 `key_events`，不新建：
  - 同一连续场景内
  - 同一类型标签（如都是「战斗」或都是「探索」）
  - 游戏内时间间隔 ≤ 5 分钟
  - 追加后 `summary` 仍能 2-4 句话概括

---

## 前情提要

> *（空白——新模组开始时 AI 在此写入）*

---

## 节点记录

### 节点格式说明

每个节点代表一个值得记录的事件或一段完成的小章节。

```markdown
node_id: S1N1
type: 类型标签
title: 一句话标题
in_game_date: 日期
summary: |
  2-4 句话概括发生了什么。可以换行，
  但保持简洁。
key_events:
  - 事件 1
  - 事件 2
  - 事件 3
character_changes:
  兰斯: 变化描述（如：HP 35→22，消耗 1 瓶药水，获得 XXX）
  卡芙卡: 变化描述
loot_acquired:
  - 物品 A
  - 物品 B
unlock_flags:
  - flag_a_获得
  - flag_b_发现
dm_notes: 给未来自己的提示：埋伏笔、待处理的剧情线、临时想法
```

> **类型标签可选值**：`剧情进展` | `战斗` | `探索` | `社交` | `角色发展` | `转折` | `商业` | `休整` | `修正`

> 追加节点可使用 `/节点` Skill。完整存档流程可使用 `/存档` Skill。

---

### 实际记录

> *（空白——新模组开始时 AI 在此追加节点）*
